<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    android:padding="@dimen/paddingText">

    <TextView android:id="@+id/recordText"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:textColor="@color/textColor"
        android:layout_marginTop="@dimen/marginTop"
        android:textSize="@dimen/subtitle"
        android:text="Record sporting activities"
        android:layout_weight="0"
        android:layout_gravity="center"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:orientation="vertical"
        android:layout_weight="1"
        android:gravity="bottom"
        android:layout_marginBottom="@dimen/marginTop">

        <Button android:id="@+id/btn_recordActivity"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:layout_marginTop="@dimen/marginTop"
            android:background="@color/colorPrimary"
            android:text="Record activity"
            android:textColor="@color/textColor"/>

        <Button android:id="@+id/btn_seeResults"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:layout_marginTop="@dimen/marginTop"
            android:background="@color/colorPrimary"
            android:text="See results"
            android:textColor="@color/textColor"/>

    </LinearLayout>
</LinearLayout>